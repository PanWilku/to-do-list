export default class Task {
    constructor(task, name) {
        this.task = task;
        this.name = name;
    }


    addTask(task) {

    };

    removeTask(task) {

    };

    updateTask(task) {

    };
}